# Plantilla para producción (servidor 5.161.229.77).
# En el servidor: cp .env.production.example .env y rellena DB_PASSWORD (y DATABASE_URL si usas esa forma).

# Base de datos (variables separadas, igual que en local)
DB_HOST=168.119.226.236
DB_PORT=5432
DB_NAME=yego_integral
DB_USER=yego_user
DB_PASSWORD=

# Entorno y CORS
ENVIRONMENT=production
CORS_ORIGINS=https://ct4.yego.pro

# Opcional
# ADMIN_TOKEN=
