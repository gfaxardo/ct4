# Desarrollo local (opcional).
# Copia a .env.local y ajusta: cp .env.example .env.local

# Backend CT4 (datos, pagos, identity, etc.)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# API de autenticación (login). Por defecto: https://api-int.yego.pro/api/auth/login
# NEXT_PUBLIC_AUTH_API_URL=https://api-int.yego.pro/api/auth/login
