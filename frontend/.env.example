# Desarrollo local (opcional).
# Si no existe .env.local, el frontend usa por defecto http://localhost:8000.
# Copia a .env.local y ajusta si necesitas otro backend:
# cp .env.example .env.local

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
